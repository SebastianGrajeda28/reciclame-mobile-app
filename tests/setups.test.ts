
describe("Entorno de Desarrollo - Reciclame", () => {
  test("Debería confirmar que Jest y TypeScript están configurados", () => {
    const configValida: boolean = true;
    expect(configValida).toBe(true);
  });
});
